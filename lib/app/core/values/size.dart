import 'package:flutter/material.dart';

/// kSpacingS Using for Notification padding, between input forms
const kSpacingS = EdgeInsets.all(4);
const kSpacingSV = EdgeInsets.symmetric(vertical: 4);
const kSpacingSH = EdgeInsets.symmetric(horizontal: 4);

/// Between buttons, icons and text inside menu items, between text and checkbox, radio button and switch.
const kSpacingM = EdgeInsets.all(8);
const kSpacingMV = EdgeInsets.symmetric(vertical: 8);
const kSpacingMH = EdgeInsets.symmetric(horizontal: 8);

/// Notification padding, between inputs in forms.
const kSpacingL = EdgeInsets.all(16);
const kSpacingLV = EdgeInsets.symmetric(vertical: 16);
const kSpacingLH = EdgeInsets.symmetric(horizontal: 16);

/// Modal and card padding.
const kSpacingXL = EdgeInsets.all(24);
const kSpacingXLV = EdgeInsets.symmetric(vertical: 24);
const kSpacingXLH = EdgeInsets.symmetric(horizontal: 24);

/// Between page sections.
const kSpacingXXL = EdgeInsets.all(48);
const kSpacingXXLV = EdgeInsets.symmetric(vertical: 48);
const kSpacingXXLH = EdgeInsets.symmetric(horizontal: 48);

const kPadding8 = EdgeInsets.all(8);
const kPadding8H = EdgeInsets.symmetric(horizontal: 8);
const kPadding8V = EdgeInsets.symmetric(vertical: 8);
const kPadding16 = EdgeInsets.all(16);
const kPadding16H = EdgeInsets.symmetric(horizontal: 16);
const kPadding16V = EdgeInsets.symmetric(vertical: 16);
const kPadding8H16V = EdgeInsets.symmetric(horizontal: 8, vertical: 16);
const kPadding16H8V = EdgeInsets.symmetric(horizontal: 16, vertical: 8);